[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every URL serves index.html so React Router can
# resolve client-side routes like /incident/:id on direct load.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
